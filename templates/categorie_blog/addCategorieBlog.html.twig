{% extends 'dashboard-base.html.twig' %}


{% block title %}New Category{% endblock %}

{% block nav %}
                <div class="navbar-nav w-100">
                    <a href="index.html" class="nav-item nav-link active"><i class="fa fa-tachometer-alt me-2"></i>Dashboard</a>
                    <div class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown"><i class="fa fa-laptop me-2"></i>Category Management</a>
                        <div class="dropdown-menu bg-transparent border-0">
                            <a href="{{ path('app_new_categorie_blog')}}" class="dropdown-item">create type</a>
                            <a href="{{ path('app_categorie_blog')}}" class="dropdown-item">list type</a>
                        </div>
                    </div>
                    
                </div>
            </nav>
        </div>

        <!-- Sidebar End -->
{% endblock %}
{% block body %}
 <h1>New Category </h1>

                      {{form_start(f,{'attr': {'novalidate': 'novalidate'}} )}}
                            <div class="row">
                                <div class="col-lg-6">
                                {# {{ form_label (form.nomCategorie, "Category name") }}
                                {{ form_widget(form.nomCategorie) }}
                                 {{ form_errors (form.nomCategorie) }} #}
                                 {{ form_label(f.nomCategorie, "Category name") }} 
                                    {{ form_widget(f.nomCategorie) }} 
                                    {{ form_errors(f.nomCategorie) }} 
                                </div>
                               
                            </div><!-- /.row -->
                            <button class="btn">Create</button>
                            <a href="{{ path('app_categorie_blog') }}">Retour</a>
                    {{form_end(f)}}
                     
                 
                    </div><!-- /.col-lg-8 -->
                   
                </div><!-- /.row --></div>
            </div><!-- /.container -->
        </div><!-- /.contact-one -->


        
{% endblock %}