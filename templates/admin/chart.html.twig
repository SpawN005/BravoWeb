{% extends 'back2.html.twig' %}
{% block body %}

 <!-- Sidebar Start -->
   {% block sidebar %} <div class="sidebar pe-4 pb-3">
        <nav class="navbar bg-secondary navbar-dark">
            <a href="index.html" class="navbar-brand mx-4 mb-3">
                <h3 class="text-primary"><i class="fa fa-user-edit me-2"></i>Tun'ART</h3>
            </a>
            <div class="d-flex align-items-center ms-4 mb-4">
                <div class="position-relative">
                                     <img class="rounded-circle" src="{{ asset('/uploads/images/' ~ app.user.image) }}" alt="" style="width: 40px; height: 40px;">


                    <div class="bg-success rounded-circle border border-2 border-white position-absolute end-0 bottom-0 p-1"></div>
                </div>
                <div class="ms-3">
                    <h6 class="mb-0">{{app.user.firstName}} {{app.user.lastName}}</h6>
                    <span>Admin</span>
                </div>
            </div>
            <div class="navbar-nav w-100">
                <a href="index.html" class="nav-item nav-link "><i class="fa fa-tachometer-alt me-2"></i>Dashboard</a>


                    <a class="nav-item nav-link " href="{{path('adminbacks')}}"   ><i class="fa fa-keyboard me-2"></i>
                        <span >User</span>
                    </a>
                    <!-- <a class="nav-item nav-link " href="{{path('adminbacks')}}"   ><i class="fa fa-keyboard me-2"></i>
                        <span >Charts</span>
                    </a> -->

                    


                <div class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown"><i class="far fa-file-alt me-2"></i>Events</a>
                    <div class="dropdown-menu bg-transparent border-0">
                        <a href="signin.html" class="dropdown-item"> Create Event</a>
                        <a href="signup.html" class="dropdown-item">Check Event Status</a>
                    </div>
                </div>                
                <a href="{{ path('app_chart') }}" class="nav-item nav-link active" ><i class="fa fa-chart-bar me-2"></i>Charts</a>

            </div>
        </nav>
    </div>{% endblock %}

<body>
    <div class="container-fluid position-relative d-flex p-0">
        <!-- Spinner Start -->
        


            <!-- Chart Start -->
            <div class="container-fluid pt-4 px-4">
                <div class="row g-4">
                    <div class="col-sm-12 col-xl-6">
                        <div class="bg-secondary rounded h-100 p-4">
                            <h6 class="mb-4">Chart of Banned users </h6>
<canvas id="myChart2"></canvas>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<script>
  const chartData22 = {
    banned: 0,
    notBanned: 0,
  };

  {% for k in user %}
    {% if k.isBanned() %}
      chartData22["banned"] += 1;
    {% else %}
      chartData22["notBanned"] += 1;
    {% endif %}
  {% endfor %}

  const ctxss = document.getElementById('myChart2');

  new Chart(ctxss, {
    type: 'pie',
    data: {
      labels: ["Banned", "Not Banned"],
      datasets: [{
        label: 'Banned users',
        data: [chartData22["banned"], chartData22["notBanned"]],
        backgroundColor: [
          "rgba(235, 22, 22, .7)",
          "rgba(60, 179, 113, .7)"
        ]
      }]
    }
  });
</script>

                        </div>
                    </div>








                    <div class="col-sm-12 col-xl-6">
                        <div class="bg-secondary rounded h-100 p-4">
                            <h6 class="mb-4">Statistics about Verified users</h6>
                            <canvas id="myChart3"></canvas>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<script>
  const chartData = {};

  {% for k in user %}
    if ("{{ k.isVerified }}" === "1") {
      if (!chartData["Verified"]) {
        chartData["Verified"] = 1;
      } else {
        chartData["Verified"]++;
      }
    } else {
      if (!chartData["Unverified"]) {
        chartData["Unverified"] = 1;
      } else {
        chartData["Unverified"]++;
      }
    }
  {% endfor %}

  const ctx = document.getElementById('myChart3');

  new Chart(ctx, {
    type: 'doughnut',
    data: {
      labels: Object.keys(chartData),
      datasets: [{
        label: 'reference',
        data: Object.values(chartData),
        backgroundColor: [
          "rgba(54, 162, 235, 0.7)",
          "rgba(54, 162, 235, 0.4)"
        ]
      }]
    }
  });
</script>


                        </div>
                    </div>




                    <div class="col-sm-12 col-xl-6">
                        <div class="bg-secondary rounded h-100 p-4">
                            <h6 class="mb-4">Names of Users:</h6>
                            <canvas id="myChart4"></canvas>
                            <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                            <script>
                            const chartData4 = {};

                            {% for k in user %}
                                if (!chartData4["{{ k.firstname }}"]) {
                                chartData4["{{ k.firstname }}"] = {{ k.id }};
                                } else {
                                chartData4["{{ k.firstname }}"] += {{ k.id }};
                                }
                            {% endfor %}

                            const ctx22 = document.getElementById('myChart4');

                            new Chart(ctx22, {
                                type: 'doughnut',
                                data: {
                                labels: Object.keys(chartData4),
                                datasets: [{
                                    label: 'reference',
                                    data: Object.values(chartData4),
                                    backgroundColor: [
                                    "rgba(235, 22, 22, .7)",
                                                "rgba(235, 22, 22, .6)",
                                                "rgba(235, 22, 22, .5)",
                                                "rgba(235, 22, 22, .4)",
                                                "rgba(235, 22, 22, .3)"
                                    ]
                                }]
                                }
                            });
                            </script>
                                                    
                        </div>
                    </div>







                    <div class="col-sm-12 col-xl-6">
                        <div class="bg-secondary rounded h-100 p-4">
                            <h6 class="mb-4">Number of Active users</h6>
                            {% if user is defined and user is iterable and user|length > 0 %}
                                <p>Total registered users: {{ user|length }}</p>
                            {% else %}
                                <p>No registered users found</p>
                            {% endif %}
                        </div>
                    </div>



            <!-- Chart End -->


            <!-- Footer Start -->
            <div class="container-fluid pt-4 px-4">
                <div class="bg-secondary rounded-top p-4">
                    <div class="row">
                        <div class="col-12 col-sm-6 text-center text-sm-start">
                            &copy; <a href="#">Tun'ART </a>, All Right Reserved. 
                        </div>
                        <div class="col-12 col-sm-6 text-center text-sm-end">
                            <!--/*** This template is free as long as you keep the footer author’s credit link/attribution link/backlink. If you'd like to use the template without the footer author’s credit link/attribution link/backlink, you can purchase the Credit Removal License from "https://htmlcodex.com/credit-removal". Thank you for your support. ***/-->
                            Designed By <a href="https://htmlcodex.com">HTML Codex</a>
                            <br>Distributed By: <a href="https://themewagon.com" target="_blank">ThemeWagon</a>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Footer End -->
        </div>
        <!-- Content End -->


        <!-- Back to Top -->
        <a href="#" class="btn btn-lg btn-primary btn-lg-square back-to-top"><i class="bi bi-arrow-up"></i></a>
    </div>

    <!-- JavaScript Libraries -->
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>


    <!-- Template Javascript -->
    <script src="js/main.js"></script>
</body>

{% endblock %}